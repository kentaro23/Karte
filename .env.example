# Copy to .env :  cp .env.example .env
# Local dev cluster (scripts/pg.sh) or docker compose both use this URL/port.
DATABASE_URL="postgresql://postgres@127.0.0.1:54329/medixus_karte?schema=public"

# Session signing secret — change for any non-local use.
AUTH_SECRET="dev-only-medixus-karte-secret-change-me"
NODE_ENV="development"
