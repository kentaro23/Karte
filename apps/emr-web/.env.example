# Copy to apps/emr-web/.env :  cp apps/emr-web/.env.example apps/emr-web/.env
# Next.js loads env from the app directory, so this mirrors the root .env.
DATABASE_URL="postgresql://postgres@127.0.0.1:54329/medixus_karte?schema=public"
AUTH_SECRET="dev-only-medixus-karte-secret-change-me"
